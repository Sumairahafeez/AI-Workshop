a=[1,2,3]
print(a+[2,3,4])//this will concatenate the lists
#ists can be mutable it can be modififed and appended but the tuple cannot be appended
#dict assigns all the variables some specific values
fa = {"A":1, "B":2}
print(fa)
#this is like a class in oop or csharp it is called hashmap in c++
#set datatype  records only one instance of any variables
a = 1
if a == 1
    print("true")
elif a== 2
    print("false")
else:
    pass #it is a though process and dont give error it just passes the operator
    #loops 
    for i in range(10):
    # if you want to give th estarting point
    for in in range(1,10):
    # if you want to give steps too
    for i in range(1,10,2) # move in two steps from 1 to 10
    a = [i for i in range(10)]
    print(a)
    # if you want an array to initially fill the array with 0
    a = [0]*10
    while True:
        # for function definition we use def
def add(a,b):
    return a+b
    # functions should be define above before calling the fucntion
a = [i for i in range(10)]
# if you want to include a part of array in another part 
b = a[1:6]# if you want to include 5 write till 6 last int is not inluded
b= a[10:-1:-1]
b = a[::-1]# this is called list comprehension if you put the first and second part empty it will bydefault begin by starting and take till end
#if you want to copy the whole array
b = a[:]
#if you give starting index
b = a[1:] # by deafult it will accept ityself
#if you want to reverse the wholw array
b = a[::-1]
# if you want to import 


